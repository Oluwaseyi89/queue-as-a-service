# Generated by Cargo
target
**/*.rs.bk

# VCS
.git
.github

# Observability/compose config is mounted as volumes at runtime, not
# baked into the qaas-server image, so it doesn't need to be in the
# build context.
docker

# Docs
*.md
