[package]
name = "qaas-types"
description = "Shared wire types (message envelope, identifiers, error types) used by the QaaS core engine, server, and client SDK."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
# Not ready for crates.io yet — a workspace-internal path dependency of
# qaas-core/qaas-server/qaas-client for now. Revisit once there's an
# actual reason to publish it standalone.
publish = false

[lints]
workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
uuid.workspace = true
thiserror.workspace = true
